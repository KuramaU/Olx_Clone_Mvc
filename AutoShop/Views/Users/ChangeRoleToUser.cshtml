@* @model Data.Entities.User

<h1>Change User Role to User</h1>

<h3>Are you sure you want to change this user's role to User?</h3>
<div>
    <h4>User Details</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            Email:
        </dt>
        <dd class="col-sm-10">
            @Model.Email
        </dd>
        <!-- Додайте інші властивості користувача, які ви хочете відобразити -->
    </dl>

    <form asp-action="ChangeRoleToUser">
        <input type="hidden" asp-for="Email" />
        <div class="form-group">
            <input type="submit" value="Make User" class="btn btn-success" />
            <a asp-action="Index" class="btn btn-secondary">Cancel</a>
        </div>
    </form>
</div>
 *@