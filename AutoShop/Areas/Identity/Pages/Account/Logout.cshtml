@page
@model LogoutModel
@{
    ViewData["Title"] = "Вийти";
}

<header>
    <h1>@ViewData["Title"]</h1>
    @{
        if (User.Identity?.IsAuthenticated ?? false)
        {
            <form class="form-inline" asp-area="Identity" asp-page="/Account/Logout" asp-route-returnUrl="@Url.Page("/", new { area = "" })" method="post">
                <button type="submit" class="nav-link btn btn-link text-dark">Нажміть тут щоб вийти</button>
            </form>
        }
        else
        {
            <p>Ви вдало вийли з вашого облікового запису.</p>
        }
    }
</header>
